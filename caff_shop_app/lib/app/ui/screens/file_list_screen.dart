import 'package:flutter/material.dart';

class FileListScreen extends StatelessWidget {
  const FileListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('file_list_screen'),
      ),
    );
  }
}
