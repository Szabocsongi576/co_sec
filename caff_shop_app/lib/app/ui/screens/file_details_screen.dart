import 'package:flutter/material.dart';

class FileDetailsScreen extends StatelessWidget {
  const FileDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('file_details_screen'),
      ),
    );
  }
}
